import { UserSubmitHistory } from './user-submit-history';

describe('UserSubmitHistory', () => {
  it('should create an instance', () => {
    expect(new UserSubmitHistory()).toBeTruthy();
  });
});
